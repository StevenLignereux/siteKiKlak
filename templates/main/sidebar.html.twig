{% block body %}
    <aside class="sidebar">
        {{ render(path('categories')) }}
        {{ render(controller(
            'App\\Controller\\PostController::randomPost'
        )) }}
    </aside>
{% endblock %}

